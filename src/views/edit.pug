extends base.pug


block content 
    h4 Change Title of video
    //form(action="/save-changes")  /save-changes?title=First+Vidsddeo
    form(method="POST") 
        input(name="title", placeholder="Video Title", value=video.title)
        input(value="Save", type="submit")
